"use strict";

!function() {
    var a = angular.module("directivesOne", [ "ngResource", "ngRoute", "testControllers", "testServices", "userDirectives" ]);
    a.config([ "$routeProvider", function(a) {
        a.when("/", {
            templateUrl: "partials/test",
            controller: "testCtrl"
        }).otherwise({
            redirectTo: "/"
        });
    } ]);
}(), function() {
    var a = angular.module("testControllers", []);
    a.controller("testCtrl", [ "$scope", "testService", function(a, b) {
        a.users = b.users;
    } ]);
}(), function() {
    var a = angular.module("userDirectives", []);
    a.directive("userPanel", [ function() {
        var a = {
            restrict: "E",
            replace: !0,
            scope: {
                userInfo: "=userInfo"
            },
            templateUrl: "templates/userPanel.html"
        };
        return a;
    } ]);
}(), function() {
    var a = angular.module("testServices", []);
    a.service("testService", [ function() {
        this.users = [ {
            name: "brian",
            about: "coolio"
        }, {
            name: "bob",
            about: "coolio2"
        }, {
            name: "bobbiton",
            about: "coolio3"
        }, {
            name: "borne",
            about: "coolio4"
        } ];
    } ]);
}();